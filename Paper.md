## III. System Model
### 1. 送信機（Transmitter / Semantic Encoder）

入力画像 $x \in \mathbb{R}^{3 \times H \times W}$ と、それに対応する関心領域を示す重要度マップ $I \in [0, 1]^{1 \times H \times W}$ が与えられます。また、送信機は通信路の現在の信号対雑音比（SNR）$\gamma$ を完全なチャネル状態情報（CSI）として知っていると仮定します。

送信機 $f_{\theta}$（$\theta$ はエンコーダの学習パラメータ）は、入力画像を直接、複素（または実数）の送信シンボル（潜在特徴量） $z \in \mathbb{R}^{C \times H' \times W'}$ にマッピングします。


$$z = f_{\theta}(x, I, \gamma)$$

**【アテンションによる不等誤り保護（UEP）】**
エンコーダ内部の `ImportanceAFModule` において、中間特徴量 $F$ に対して以下の2つのゲート（重み付け）が計算され、画素単位での送信電力（振幅）の動的割り当てが行われます。

* **空間ゲート $A_{spatial}(F, I)$**: 特徴量と重要度マップを結合して計算され、ピクセルごとの振幅をスケーリングします（重要な領域の振幅を大きくする）。
* **チャネルゲート $A_{channel}(F, I, \gamma)$**: 特徴量の全体平均、重要度の平均、および SNR $\gamma$ から計算され、特徴次元（チャネル）ごとのリソース配分を決定します。

### 2. 通信路モデル（Wireless Channel）

送信シンボル $z$ は無線通信路を介して送信されます。本システムでは加法性白色ガウス雑音（AWGN）通信路、またはレイリーフェージング通信路を想定します。AWGNの場合、受信シンボル $\hat{z}$ は以下のように表されます。


$$\hat{z} = z + n$$


ここで、$n \sim \mathcal{N}(0, \sigma^2 \mathbf{I})$ は分散 $\sigma^2$ のガウス雑音であり、ノイズ電力 $\sigma^2$ は与えられた SNR $\gamma$ と信号の平均電力に基づいて決定されます。

### 3. 受信機（Receiver / Semantic Decoder）

本システムの最大の特長は、**受信機が重要度マップ $I$ を一切必要としない（通信オーバーヘッドがゼロである）**点です。

受信機 $g_{\phi}$（$\phi$ はデコーダの学習パラメータ）は、ノイズが重畳された受信シンボル $\hat{z}$ と、SNR $\gamma$ のみを用いて、元の画像を再構成します。


$$\hat{x} = g_{\phi}(\hat{z}, \gamma)$$

デコーダ内部の標準的なアテンションモジュール（`AFModule`）は、重要度マップなしで受信信号の振幅と $\gamma$ から最適なスケーリングを行い、最終的な再構成画像 $\hat{x}$ を出力します。

### 4. 提案する最適化目標（Optimization & Loss Function）

本システム全体をEnd-to-Endで学習させるため、従来の再構成誤差（MSE）に加えて、**「重要度マップ $I$ と誤差マップ $E$ のピアソン相関（Pearson Correlation）」**を正則化項として導入します。

まず、相関正則化に用いるピクセルごとの誤差マップ $E \in \mathbb{R}^{1 \times H \times W}$ を次のように定義します。
本実装では、相関項の安定性を優先し、チャネル平均の **絶対誤差（MAE）** を用いる。

$$E_{u, v} = \frac{1}{3} \sum_{c=1}^{3} \left|x_{c, u, v} - \hat{x}_{c, u, v}\right|$$

次に、重要度マップ $I$ と誤差マップ $E$ の間のピアソン相関係数 $\rho(I, E)$ を計算します。

$$\rho(I, E) = \frac{\sum (I - \bar{I})(E - \bar{E})}{\sqrt{\sum (I - \bar{I})^2 \sum (E - \bar{E})^2}}$$

（ここで $\bar{I}$ と $\bar{E}$ はそれぞれの空間平均です）

最終的な損失関数 $\mathcal{L}_{total}$ は、再構成損失 $\mathcal{L}_{recon}$ と相関正則化項の線形和として定義され、これを最小化するようにパラメータ $\theta, \phi$ を最適化します。再構成損失は **L1（MAE）** を標準設定とし、必要に応じて MSE に切り替え可能とする。

$$\min_{\theta, \phi} \; \mathcal{L}_{total} = \mathcal{L}_{recon} + \lambda_{corr} \cdot \rho(I, E)$$


**【この数式が意味する強力なロジック】**
相関係数 $\rho(I, E)$ は $-1$ から $1$ の値を取ります。損失関数としてこの値を最小化（マイナスへ誘導）しようとすることで、ネットワークは**「重要度 $I$ が高い場所ほど、誤差 $E$ を小さくしなければならない」**という強い制約を受けます。これにより、エンコーダ $f_{\theta}$ は自発的に重要領域へ送信電力を集中させる（UEPを獲得する）ようになります。



## IV. Experimental Setup

### A. Dataset and Preprocessing

本評価では、高解像度顔画像データセットである **FFHQ (Flickr-Faces-HQ)** を用いて学習および評価を行った。学習には 70,000 枚の画像を使用した。評価は、同一データディレクトリからランダムに抽出したサンプル（例: 20 枚）に対して、固定 SNR（例: 0 dB）および複数 SNR（5/0/-10 dB）で個別に推論を実行し、指標を集計した。
入力画像はすべて $256 \times 256$ ピクセルにリサイズしてネットワークに入力した。また、関心領域（ROI）を示す重要度マップ $I \in [0, 1]^{1 \times 256 \times 256}$ は、特定の顔認識モデルに依存しない汎用的な評価を行うため、ランダムな空間構造（ガウス分布に基づくブロブやパッチなど）を持つ合成マップを生成して付与した。

### B. Implementation Details of the Proposed Method

提案手法である **Importance-aware ADJSCC** の実装において、エンコーダおよびデコーダは、非線形活性化関数として PReLU（Parametric ReLU）と GDN（Generalized Divisive Normalization）を組み合わせた5層の畳み込み層で構成されている。

* **アテンション機構**: エンコーダ側の全層に提案する `ImportanceAFModule` を適用し、空間的な重要度マップ $I$ とチャネル状態情報（SNR: $\gamma$）を基に送信電力の動的割り当てを行った。一方、デコーダ側には重要度マップを必要としない標準的な `AFModule` を配置した。
* **通信路（Channel）設定**: 無線通信路として加法性白色ガウス雑音（AWGN）通信路を想定し、学習時の SNR は一定範囲からランダムにサンプリングしてロバスト性を高めた（Multiple SNR training）。送信シンボルのチャネル次元数 $C$ は通信帯域幅の制約に応じて設定した（例：$C=2$ 等）。
* **最適化パラメータ**: 損失関数 $\mathcal{L}_{total} = \mathcal{L}_{recon} + \lambda_{corr} \cdot \rho(I, E)$ の最小化には Adam オプティマイザを使用し、再構成損失 $\mathcal{L}_{recon}$ には L1（Mean Absolute Error; MAE）を標準設定として採用し、必要に応じて MSE に切り替えた。また、相関正則化項の重み $\lambda_{corr}$ は、再構成品質と不等誤り保護（UEP）性能のトレードオフを最適化するように経験的に設定した。

**学習設定（本稿の実験コマンドに基づく）**: 画像サイズは $256\times256$、バッチサイズ 8、最適化は Adam、学習率 $1\times10^{-4}$、エポック数 50 とした。通信帯域はチャネル数 $C=2$ とし、Multiple-SNR 学習として $\gamma \sim \mathcal{U}[-10, 10]$ dB から各ミニバッチでサンプリングした。提案法の相関重みは $\lambda_{corr}=0.1$ を用いた。



### C. Baseline Methods

提案手法の有効性を検証するため、以下のベースライン手法との比較を行った。

1. **Standard ADJSCC (Xu et al., 2021)**
* **実装方法**: 提案手法から重要度マップの入力（`ImportanceAFModule` および損失関数の相関正則化項 $\rho(I, E)$）を完全に排除し、従来のチャネル・空間アテンション（`AFModule`）のみを用いて、相関正則化を無効化（$\lambda_{corr}=0$）し、重要度ゲーティングも無効化して学習したモデル。再構成損失は提案法と同様に L1 を標準とした。これにより、「重要度マップを用いたエンコーダ側での送信電力制御」の純粋な効果を測定する。

**比較用 Standard 設定**: 比較実験では、計算資源の都合により Standard モデルは 6 エポックで学習を実行した（提案法は 50 エポック）。厳密比較のためにはエポック数を揃えた再学習も実施すべきである。


2. **Traditional Separation-based Schemes (参考ベースライン)**
* **実装方法**: 従来の「情報源符号化＋通信路符号化」の分離定理に基づく手法。例えば、画像圧縮規格（JPEG, BPG, VVCなど）と、誤り訂正符号（LDPC, Turbo符号）および変調方式（QAMなど）を組み合わせたシステム。これらは、重要度マップを別途サイド情報として伝送するための通信オーバーヘッド（ビットレートの消費）を伴うモデルとして理論的比較の対象とする。



### D. Evaluation Metrics

システムの再構成品質と、重要度に応じた不等誤り保護（UEP）の達成度を定量化するため、以下の指標を用いた。

* **PSNR (Peak Signal-to-Noise Ratio)** および **LPIPS**: 画像全体の平均的な物理的・知覚的再構成品質を評価。
* **Split-MSE (Region-specific MSE)**: 重要度マップの閾値（例: $I \geq 0.5$）を設定し、画像領域を「High-Importance（高重要度）」と「Low-Importance（低重要度）」に分割して個別に MSE を算出。提案手法が高重要度領域において、いかに優先的かつ正確に画像を復元できているか（UEP性能）を示す。
* **Pearson Correlation ($\rho$)**: 推論時における、重要度マップ $I$ と誤差マップ $E$（チャネル平均の絶対誤差）とのピアソン相関係数。これが負に大きいほど、重要度が高い領域の誤差が選択的に小さくなっていることを証明する。






## V. Results and Discussion

本節では、異なるチャネル環境（$\text{SNR} = 5\text{ dB}, 0\text{ dB}, -10\text{ dB}$）における提案手法（Importance-aware ADJSCC）と標準的なADJSCC（Standard）の性能比較について考察する。表 I に定量評価の結果を示す。
| SNR | Method | PSNR (dB) | LPIPS ↓ | CORR | MSE ($\geq 0.5$) ↓ | MSE ($< 0.5$) |
| --- | --- | --- | --- | --- | --- | --- |
| **5 dB** | Standard | **25.04** | 0.318 | -0.021 | 0.00331 | **0.00353** |
|  | **Proposed** | 20.96 | **0.308** | **-0.713** | **0.00208** | 0.01086 |
| **0 dB** | Standard | **23.23** | **0.370** | -0.020 | 0.00491 | **0.00514** |
|  | **Proposed** | 19.56 | 0.398 | **-0.653** | **0.00324** | 0.01480 |
| **-10 dB** | Standard | **18.34** | **0.601** | -0.026 | 0.01492 | **0.01524** |
|  | **Proposed** | 16.61 | 0.643 | **-0.391** | **0.01094** | 0.02722 |

### A. Unequal Error Protection (UEP) Performance

提案手法の最大の目的である「関心領域（ROI）の優先的保護」は、重要度閾値に基づく Split-MSE の結果から明確に実証された。表 I が示す通り、重要度が高い領域（$\text{MSE} \geq 0.5$）において、提案手法はすべてのSNR条件でStandard手法を大幅に上回る（誤差が小さい）復元精度を達成している。

例えば $\text{SNR} = 5\text{ dB}$ において、Standard手法の高重要度領域の MSE が 0.00331 であるのに対し、提案手法は **0.00208（約37%の誤差削減）** を達成した。過酷な通信環境である $-10\text{ dB}$ においても、0.01492 から 0.01094 へと確実な誤差低減が見られる。これは、提案するエンコーダ内の `ImportanceAFModule` が、受信機へ付加情報を送ることなく、送信側での特徴量の振幅制御のみで実効的な UEP を実現していることを示している。

### B. Effectiveness of Correlation Loss

提案手法が自発的な UEP を獲得できた理由は、重要度マップ $I$ と誤差マップ $E$ 間のピアソン相関係数（CORR: $\rho$）の推移から裏付けられる。

Standard手法では、すべての SNR において CORR がほぼゼロ（$-0.02$ 付近）であり、画像の重要度と復元誤差の間に相関がない（画面全体を均等に復元している）ことがわかる。対照的に、提案手法では $\text{SNR} = 5\text{ dB}$ で **$\rho = -0.713$** という極めて強い負の相関を示している。これは、提案した損失関数の正則化項（$\lambda_{corr} \cdot \rho(I, E)$）が正しく機能し、「重要度が高い（$I$ が大きい）ピクセルほど、再構成誤差（$E$）が小さくなる」というネットワークの振る舞いを強力に誘導した結果である。

### C. Trade-off in Global Reconstruction Quality

表 I において、画像全体の再構成品質を示す PSNR は、すべての条件下で提案手法がStandard手法を下回っている（例: $5\text{ dB}$ 時に $20.96\text{ dB}$ vs $25.04\text{ dB}$）。しかし、これは本手法の欠陥ではなく、**送信電力制約下における意図的なリソース再配分（トレードオフ）の結果**である。

提案手法の低重要度領域の誤差（$\text{MSE} < 0.5$）を見ると、Standard手法と比較して誤差が増大している（$5\text{ dB}$ 時に $0.01086$ vs $0.00353$）。すなわちネットワークは、限られた送信電力（通信帯域）の中で、背景などの「重要でない領域」に割り当てる電力を削減し、その分のエネルギーを「重要な領域」の保護に全振りしているのである。MSEは画像全体の平均誤差を計算する性質上、背景部分の誤差増大に引っ張られて全体PSNRは低下するが、LPIPS（知覚的品質）を見ると $5\text{ dB}$ 時には提案手法がStandard手法を僅かに上回っており（$0.308$ vs $0.318$）、人間の視覚特性においては重要な領域が鮮明である方が全体的な品質が高く知覚される可能性を示唆している。

### D. Robustness to Extreme Noise (-10 dB)

極めてノイズの大きい $\text{SNR} = -10\text{ dB}$ 環境下では、提案手法の相関係数（CORR）は $-0.391$ と絶対値が低下し、LPIPSもStandard手法に劣る結果となった。これは、チャネルノイズが大きすぎる場合、重要領域へ電力を集中させたとしてもノイズフロアに埋もれてしまい、デコーダ側でのアテンション機構によるスケール復元が困難になるためと考えられる。しかし、そのような極限状態においても、高重要度領域の MSE（0.01094）は依然としてStandard手法（0.01492）より優れており、提案手法のロバスト性が確認できる。

